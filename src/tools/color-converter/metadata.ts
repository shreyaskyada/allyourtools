import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Color Converter",
  "Convert between HEX, RGB, HSL, and CMYK color formats instantly. Visualize colors and generate corresponding CSS code. 100% client-side."
);
