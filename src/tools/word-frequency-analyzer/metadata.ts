import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Word Frequency Analyzer",
  "Analyze text for keyword density, word frequencies, and instantly find duplicate words. Perfect for SEO optimization and editing. 100% client-side."
);
