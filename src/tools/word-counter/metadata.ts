import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Word Counter",
  "Analyze and count words, characters, sentences, paragraphs, reading and speaking time, and check word frequencies in real-time."
);
