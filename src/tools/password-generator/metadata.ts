import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Password Generator",
  "Generate secure, random passwords with custom length, character sets, and a real-time strength indicator. 100% client-side — your passwords are never sent anywhere."
);
