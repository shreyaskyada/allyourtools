import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "JSON Formatter & Validator",
  "Format, validate, beautify, and minify JSON data instantly. Clean client-side JSON parsing with syntax highlighting and error checking."
);
