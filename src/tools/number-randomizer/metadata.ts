import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Random Number Generator",
  "Generate truly random numbers or sequences instantly. Customize ranges, length, and formatting for gaming, lotteries, or data generation."
);
