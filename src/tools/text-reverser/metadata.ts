import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Text Reverser",
  "Reverse text in multiple ways instantly: flip upside down, reverse words, reverse sentences, or reverse individual characters. 100% client-side."
);
