import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "List Randomizer",
  "Shuffle and randomize lists instantly. Great for generating random selections, unbiased ordering, or fair giveaways. 100% client-side."
);
