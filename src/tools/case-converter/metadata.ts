import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Case Converter",
  "Instantly convert text cases between UPPERCASE, lowercase, Title Case, Sentence case, camelCase, snake_case, PascalCase, and execute document cleanups."
);
