import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Timestamp Converter",
  "Convert Unix timestamps (seconds and milliseconds) to human-readable dates in local and UTC timezones, or convert dates back to Unix epoch timestamps instantly."
);
