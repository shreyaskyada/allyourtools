import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "URL Encoder / Decoder",
  "Encode or decode strings to make them URL-safe. Instantly translates special characters into percent-encoding and back. 100% client-side."
);
