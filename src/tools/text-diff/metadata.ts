import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Text Diff",
  "Find differences between two texts instantly. Compare plain text or code to see exactly what changed with visual highlighting. 100% client-side."
);
