import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "QR Code Generator",
  "Generate custom QR codes for URLs, text, or contact info instantly. Customize colors and download as PNG. 100% client-side."
);
