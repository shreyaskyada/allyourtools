import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Lorem Ipsum Generator",
  "Generate placeholder text for your designs and documents instantly. Customize by paragraphs, sentences, or words. 100% client-side."
);
