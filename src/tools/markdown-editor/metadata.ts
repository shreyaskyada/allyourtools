import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Markdown Editor & Live Previewer | Free Online Tool",
  "A fast, free online markdown editor with a live HTML preview. Supports GitHub-flavored markdown (GFM), tables, code blocks, and exporting to HTML."
);
